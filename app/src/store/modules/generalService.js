const service = require('@/api/httpService')
import axios from 'axios'

const state = {
}

const getters = {
}

const mutations = {
}

const actions = {

}

export default {
    state: state,
    mutations: mutations,
    getters: getters,
    actions: actions
}
